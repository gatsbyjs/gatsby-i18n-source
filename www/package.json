{
  "name": "langcode.gatsbyjs.org",
  "description": "Gatsby's Website in [Language]",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "dotenv": "^8.2.0",
    "gatsby": "^2.21.37",
    "gatsby-theme-gatsbyjs-website": "0.0.0",
    "react": "^16.13.0",
    "react-dom": "^16.13.0"
  },
  "scripts": {
    "build": "gatsby build",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "develop": "gatsby develop",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "jest",
    "stylelint": "stylelint './src/**/*.js'"
  },
  "workspaces": [
    "packages/*"
  ]
}
